EXERCISES

1. echo 'Hello World'

2. mkdir new_dir

3. rm -r new_dir

4. cp sed.txt /home/marta/Desktop/Ironhack/dataptmad0420/extra-work/m1/lab-bash-master/lorem-copy

5. cp lorem/{at.txt,lorem.txt} lorem-copy/.

6. cat sed.txt

7. cat {at.txt,lorem.txt}

8. head -n 3 sed.txt

9. tail -n 3 sed.txt

10. nano sed.txt

11. tail -n 3 sed.txt

12. sed -i 's/et/ET/g' "at.txt"

13. who

14. pwd

15. ls *.txt

16. wc -l sed.txt

17. find -type f -name "lorem*" | wc -l

18. grep -o "et" at.txt | wc -l

19. grep -o " et " at.txt | wc -l

20. grep -ro " et " | wc -l
it's also counting this file, I knew it using:
find -printf 'echo "$(grep -o " et " %p | wc -l) %p";' | sh

BONUS:

1. #!/bin/bash
name="Marta"
echo $name

2. mkdir $name
rm -r $name

3. for i in $(ls); do cat $i; done;

wc -w *.txt
using a loop:
for i in $(ls); do wc -w $i; done;

for i in $(ls); do echo "$i has $(wc -w < $i) characters lenght"; done

4. lscpu

5. [I don't want to create alias, also tried it and didn't make it work]
I tried it by creating a new file: .bash_aliases in ~/ and adding:
noquieroalias="pwd"
Saving that file and running:
if [ -f ~/.bash_aliases ]; then
>     . ~/.bash_aliases
> fi
And:
source ~/.bash_aliases


6. tar -zcvf lorem-compressed.tar.gz {lorem,lorem-copy} 
To extract it I copied it to a different folder:
tar -xzvf lorem-compressed.tar.gz
And worked



